<div class="explore-grid">
  <a href="/about" class="explore-card">
    <div class="header">MODULE_01 // PROFILE</div>

    <span class="number"> 01 </span>

    <h3>ABOUT</h3>

    <p>PERSONNEL_DOSSIER</p>

    <div class="access">ACCESS_MODULE →</div>

    <div class="stripe">
      <div></div>
      <div></div>
      <div></div>
    </div>
  </a>

  <a href="/projects" class="explore-card">
    <div class="header">MODULE_02 // WORKS</div>

    <span class="number"> 02 </span>

    <h3>PROJECTS</h3>

    <p>FABRICATION_ARCHIVE</p>

    <div class="access">ACCESS_DATABASE →</div>

    <div class="stripe">
      <div></div>
      <div></div>
      <div></div>
    </div>
  </a>

  <a href="/gallery" class="explore-card">
    <div class="header">MODULE_03 // VISUAL</div>

    <span class="number"> 03 </span>

    <h3>ART</h3>

    <p>VISUAL_ASSET_MATRIX</p>

    <div class="access">OPEN_ARCHIVE →</div>

    <div class="stripe">
      <div></div>
      <div></div>
      <div></div>
    </div>
  </a>

  <a href="/notes" class="explore-card">
    <div class="header">MODULE_04 // KNOWLEDGE</div>

    <span class="number"> 04 </span>

    <h3>NOTES</h3>

    <p>RESEARCH_DATABASE</p>

    <div class="access">LOAD_DOCUMENTS →</div>

    <div class="stripe">
      <div></div>
      <div></div>
      <div></div>
    </div>
  </a>
</div>

<style>
  .explore-grid {
    max-width: 1400px;

    margin: 40px auto 120px;

    padding: 0 40px;

    display: grid;

    grid-template-columns: repeat(4, 1fr);

    gap: 25px;
  }

  .explore-card {
    position: relative;

    min-height: 260px;

    padding: 25px;

    background: rgba(8, 8, 12, 0.9);

    border: 1px solid var(--border);

    font-family: var(--font-mono);

    overflow: hidden;

    transition:
      transform 0.3s,
      border-color 0.3s,
      box-shadow 0.3s;
  }

  /* corner */

  .explore-card::before {
    content: "";

    position: absolute;

    right: 0;

    top: 0;

    width: 45px;

    height: 45px;

    background: var(--accent);

    clip-path: polygon(100% 0, 100% 100%, 0 0);
  }

  .header {
    font-size: 10px;

    letter-spacing: 2px;

    color: #777;

    border-bottom: 1px dashed var(--border);

    padding-bottom: 12px;
  }

  .number {
    display: block;

    margin-top: 35px;

    font-size: 12px;

    font-weight: bold;

    color: var(--accent);
  }

  h3 {
    margin-top: 15px;

    font-size: 34px;

    font-weight: 900;

    color: white;

    letter-spacing: -1px;
  }

  p {
    margin-top: 8px;

    font-size: 11px;

    letter-spacing: 1px;

    color: #aaa;
  }

  .access {
    position: absolute;

    bottom: 55px;

    left: 25px;

    font-size: 11px;

    font-weight: bold;

    color: var(--terminal-green);
  }

  .stripe {
    position: absolute;

    bottom: 25px;

    left: 25px;

    height: 5px;

    width: 90px;

    display: flex;
  }

  .stripe div {
    flex: 1;
  }

  .stripe div:nth-child(1) {
    background: var(--stripe-red);
  }

  .stripe div:nth-child(2) {
    background: var(--stripe-yellow);
  }

  .stripe div:nth-child(3) {
    background: var(--stripe-teal);
  }

  .explore-card:hover {
    transform: translateY(-8px);

    border-color: var(--accent);

    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.6);
  }

  .explore-card:hover .access {
    color: var(--accent);
  }

  @media (max-width: 1100px) {
    .explore-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 500px) {
    .explore-grid {
      grid-template-columns: 1fr;

      padding: 0 20px;
    }
  }
</style>
